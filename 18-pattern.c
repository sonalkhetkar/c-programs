#include<stdio.h>
void main()
{
    int a,b,option;
    int sum, sub, mult, div;
    printf("Enter first number :");
    scanf("%d",&a);
    printf("Enter second number :");
    scanf("%d",&b);
    printf("choose any option: ");
    if(option=1)
    {
       printf("1 for sum");
    }
    if(option=2)
    {
       printf("2 for sub");
    }
    if(option=3)
    printf("3 for mult");
    printf("4 for div");
    scanf("%d",&option);

}