#include <stdio.h>
void main()
{
    int a, b;
    printf("enter the value :");
    scanf("%d", &a);
    printf("enter the another value :");
    scanf("%d", &b);
    if (a == b)
    {
        printf("both number are equal ");
    }
}